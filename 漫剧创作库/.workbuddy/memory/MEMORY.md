# MEMORY

## 用户与协作偏好
- 用户正在建设一个 AI 漫剧/短剧创作工作区，希望我以影视制作专家、导演、编剧、监制和 AI 影视创作专家的总控视角深度参与项目制作，而不是只做局部建议。
- 用户要求：每新增一个新能力、方法论或规范，都必须进行全流水线链路闭环验证，作为 Agent 补强点。已升级为 L1/L2/L3 分层版本以降低 token 消耗。

## 项目长期事实
- 工作区 `/Users/billy/Documents/AI_pro/漫剧创作库` 是一个面向 AI 漫剧/短剧的全流程生产系统，已从 16 Agent 精简为 6 核心角色（Producer / Writer / Director / Librarian / Studio / Reviewer）。
- 关键推进原则是按 Phase Gate 从 STORY → DIRECTOR+ASSETS → STORYBOARD → GENERATION → EDIT+QA 逐阶段放行。
- 流水线链路闭环机制已升级为 v2 分层版（`docs/pipeline-sync-protocol.md`），按 L1/L2/L3 影响等级分配验证深度，~80% 变更走轻量路径，大幅降低 token 消耗。
- 项目记忆系统必须以文件方式保存在项目内：`docs/project-memory-system.md` 是文件化记忆总规范，明确对话记忆只作为辅助索引；凡影响方法论、工作流、Agent 协作、Skill 调用、模板、门禁、项目产出物状态或复盘结论的知识，都必须写入 `docs/`、`skills/`、`agents/`、`templates/`、`projects/` 等项目文件，并通过 `docs/pipeline-sync-log.md` 闭环签收。
- `docs/project-memory-system.md` 已升级为工具无关、可迁移、可交接、可恢复的系统化规范；任何新工具接手项目时，必须先读取 README、project-memory-system、workflow、agent-protocol、phase-gate、pipeline-sync、workspace-status、任务相关 docs/skills/agents/templates/projects 产出物，并完成第一轮自检后才能生产。
- 智能视界定位为漫剧创作库工业化流程的产品化 Runtime，不绑定 GPT-5.5，不强制重后端；第一版采用 Local-first、能力包 Runtime、工程目录记忆和 `.smart-vision/` 状态目录，客户端缓存只作辅助，工程文件才是客户端退出、会话结束、更换客户端和跨电脑迁移后的最高恢复依据。
- 智能视界已调整为独立平台开发工程，主目录为 `smart-vision/`；平台开发文档归档到 `smart-vision/docs/`，无限画布相关代码以复制方式迁移到 `smart-vision/canvas/legacy-workbench/` 与 `smart-vision/services/workbench/`，原始 `tools/` 与根 `studio` 保持不破坏。
- 2026-05-08 智能视界换机交接状态：当前开发重心是继续完成平台功能而非业务产物生成。已完成 Workflow Registry、Artifact Registry、Review Center、Workflow Runtime、Diagnostics、Repair、Queue/Retry/Replay、Release Registry 代码链路、Release 面板诊断展示、发布包状态机、blocked 审核保护、published 前文件存在校验，`smart-vision/app` 最近一次 `npm run build` 已通过。工程记忆权威源为 `smart-vision/outputs/.smart-vision/project-state.json`、`recovery-checkpoint.json`、`handoff-report.md`。下一步优先补齐发布前强门禁、Release 单项修复、Bridge API 端到端实测、Runner→Release 自动推进。
- 2026-05-09 智能视界平台状态：工业主链路已纠偏为 `asset_image_generation → storyboard_image_generation → video_generation → edit → qa → release`；Smart Vision 不直接调用模型，只生成和调度无限画布 Workflow JSON，并填入提示词、参考图和输出路径。已完成数据包解析、任务编排落盘、taskQueue 执行器、Release 强门禁、Runner→Release 自动推进、异常注入 smoke、状态安全写入、Workflow JSON Preflight、画布输出回写、Canvas 输出幂等与失败 retry、Pipeline Run Ledger、Pipeline Run 阶段门禁、Diagnostics 与阶段级修复入口。Runtime Diagnostics 已覆盖 Canvas 输出、Pipeline Run、Release Registry、Workflow Registry、Review Ledger、Artifact Registry review、task-run 报告、state-operation-journal 和 Workflow preflight 契约事故。最新验证：`npm run build`、`npm run smoke:runtime:strict`、`npm run smoke:anomaly` 全部通过；新增 workflow registry JSON 结构损坏、review-ledger history 异常、state-operation-journal 卡死异常注入；`pipeline_run_repair_write_fixture` 已验证损坏 Pipeline Run 可从 preview 到真实 repair 写入并 cleanup 恢复；Canvas 输出已验证 first registered / second idempotent、missing local file failed / retry registered / repeated retry idempotent；真实流水线 `pipeline-run-ep001-1778290049528` 已从 asset_images 推进到 edit，资产图回写文件为 `01-资产图与提示词/资产图/ep001/pipeline-asset-output-smoke.png`，故事板图回写文件为 `01-资产图与提示词/故事板/storyboard/ep001/pipeline-storyboard-output-smoke.png`，视频回写文件为 `03-视频/ep001/pipeline-video-output-smoke.mp4`，自动生成故事板 Workflow `draft-storyboard_image_generation-1778301596146`、视频 Workflow `draft-video_generation-1778302161828`、剪辑 Workflow `draft-edit-1778302819995`，这些 workflow preflight passed。已补 workflow 状态同步 progress-ledger，修复 canvas output 后 progress/workflow 状态不一致。
- 2026-05-09 13:33 智能视界最新闭环状态：真实流水线 `pipeline-run-ep001-1778290049528` 已完成 asset_images、storyboard_images、videos、edit、qa、release 六阶段并进入 `done`。新增剪辑产物 `05-可选输出/审核剪辑发布/edit/ep001/pipeline-edit-output-smoke.mp4`，QA Workflow `draft-qa-1778303373129`，QA 报告 `05-可选输出/审核剪辑发布/review/ep001/pipeline-qa-output-smoke.md`，Release Workflow `draft-release-1778304322583`，Release manifest `05-可选输出/审核剪辑发布/release/ep001/pipeline-release-manifest-smoke.json`。自动发布包 `release-ep001-auto-draft-qa-1778303373129` 已完成 `ready_for_review → approved → published`，publish gate blockers 为 0。后端已修正 QA/Release 审核类型映射、`qa → release` workflow 映射、release draft 去重、release workflow 输出绑定 release package，以及 artifact review done 后 workflow/progress 状态收敛。最新验证：`node --check app/scripts/bridge-server.mjs`、`npm run build`、`npm run smoke:runtime:strict`、`npm run smoke:anomaly` 全部通过，Runtime Diagnostics ready / 0 issues。下一步进入真实无限画布手动导入 / 运行联调和 UI 联调前缺口审计。
- 2026-05-09 13:56 智能视界新增 Runtime 面板 Canvas failed retry 入口：最近画布回写异常项可执行 retry dry-run 预览和确认后真实回写，结果进入最近操作摘要与详情卡；已验证 `ui-canvas-output-retry-smoke` 从 failed / Diagnostics degraded，经 preview retryable、补齐源文件 retry registered、review done 后回到 Diagnostics ready / 0 issues。`npm run build`、`npm run smoke:runtime:strict`、`npm run smoke:anomaly` 全部通过。下一步进入真实无限画布手动导入 / 运行联调。
- 2026-05-09 14:35 智能视界真实画布导入入口硬化：legacy workbench 默认端口统一为 `8877`；Bridge Snapshot 与 bundled snapshot 会按当前 Bridge 端口动态重算 workflow `importUrl/editUrl/runUrl` 和 importCheck；画布导入时 URL 参数 `bridgeBase` 优先覆盖 Workflow JSON 内嵌旧地址，避免旧工作流打到过期 Bridge。已验证 `8877` 画布入口返回 200，当前 Bridge `5199` 可读取 release workflow artifact，release workflow importCheck ok；`npm run build`、`npm run smoke:runtime:strict`、`npm run smoke:anomaly` 全部通过。注意：若继续使用默认 `5188` Bridge，需要重启旧 Bridge 进程加载新代码；否则使用快照返回的当前 `bridgeBase`。下一步进入真实浏览器画布操作联调与 UI 数据一致性检查。
- 2026-05-09 14:57 智能视界 Canvas import smoke 固化：新增 `app/scripts/canvas-import-smoke.mjs`、`npm run smoke:canvas-import`、`npm run smoke:canvas-import:live`。脚本启动临时 Bridge，检查 legacy 默认 `8877`、画布导入 `bridgeBase` 优先级、Snapshot 动态 `bridgeBase`、workflow import/edit/run URL、importCheck、workflow artifact read；live 模式要求 `8877` 真实画布页面返回 200。最新验证：`npm run smoke:canvas-import`、`npm run smoke:canvas-import:live`、`npm run build`、`npm run smoke:runtime:strict`、`npm run smoke:anomaly` 全部通过。下一步进入真实浏览器画布操作联调与 UI 数据一致性检查。
- 2026-05-09 15:27 智能视界浏览器级画布导入联调：画布页面新增 `window.__SMART_VISION_IMPORT_PROBE__`，`npm run smoke:canvas-import:browser` 会启动 Chrome headless 打开真实 `8877` runUrl，验证浏览器 JS、跨端口 Bridge fetch、workflow 导入和画布节点加载。本轮修复 Release / QA 平台 Workflow 在 legacy 画布中空白的问题，新增 `contextPack / artifactInput / workflowBuilder / phaseGate` 节点定义与只读渲染。最新验证：`draft-release-1778304322583` 浏览器导入后 `nodes 4 / conns 3`，`npm run smoke:canvas-import:browser`、`npm run build`、`npm run smoke:runtime:strict`、`npm run smoke:anomaly` 全部通过。下一步进入真实人工 RUN 与 `canvas-output` 回写联调。
- 2026-05-09 15:49 智能视界生产就绪总控：前端首页新增生产就绪总控面板，聚合 Bridge 数据源、Pipeline Run 当前阶段、Release 发布门禁、Canvas 回写异常、Workflow importCheck 和 smoke 结果，并提供端到端自测、流水线 dry-run、发布异常自测、打开画布快捷入口；UI 背景收敛为更克制的工作台风格。最新验证：`npm run build`、`npm run smoke:runtime:strict`、`npm run smoke:canvas-import:browser`、`npm run smoke:anomaly`、最终 `npm run build` 全部通过。下一步进入真实人工 RUN 与 `canvas-output` 回写联调，同时继续把 Runtime / Pipeline / Release 的高频关键操作收敛到生产就绪总控。
- 2026-05-09 16:25 智能视界当前画布执行位收敛：生产就绪总控面板新增当前阶段 Workflow 执行位，展示导入可用性、节点/连线数、workflowPath、回写目标、阶段 blocker，并提供“导入执行 / 编辑画布 / 查看 JSON”入口。该改动不直接调用模型，真实执行仍由无限画布承载。最新验证：`npm run build`、`npm run smoke:runtime:strict`、`npm run smoke:canvas-import:browser`、`npm run smoke:anomaly`、最终 `npm run build` 全部通过。下一步进入真实人工 RUN 与 `canvas-output` 回写联调。
- 2026-05-09 16:59 智能视界 canvas-output 首次回写入口：生产就绪总控新增“画布产物回写”表单，支持本地文件路径、目标 artifactPath / outputArtifactHint、可选 idempotencyKey，提供回写预览和确认回写；确认写入走 `POST /api/smart-vision/workflows/canvas-output`，物化到 outputs、登记 artifact/review，并触发 Pipeline Run 推进。最新验证：`npm run build`、`npm run smoke:runtime:strict`、`canvas-output dry-run API`、`npm run smoke:canvas-import:browser`、`npm run smoke:anomaly`、最终 `npm run build` 全部通过。下一步用真实画布 RUN 产物做一次人工回写验证。
- 2026-05-09 17:12 智能视界 canvas-output register smoke 固化：新增 `app/scripts/canvas-output-smoke.mjs` 和 `npm run smoke:canvas-output`，脚本启动临时 Bridge、生成 1x1 PNG、执行 dry-run 预览、真实 canvas-output register、Artifact Preview 读取和 Diagnostics 校验。最新验证：写入 `01-资产图与提示词/画布回写/canvas-output-register-smoke-1778317814848.png`，创建 review，Artifact Preview image / 68 bytes，Diagnostics ready / 0 issues；随后 `npm run smoke:runtime:strict`、`npm run smoke:anomaly`、最终 `npm run build` 全部通过。下一步用真实画布 RUN 产物做一次人工回写验证。
- 2026-05-09 18:27 智能视界总控 Pipeline 操作入口：生产就绪总控新增 Pipeline 控制区，支持刷新流水线、创建实例、推进阶段和修复预览；当前阶段执行位、画布导入入口、canvas-output 回写入口和 Pipeline 推进操作已收敛到同一个生产总控面板。最新验证：`npm run build`、`npm run smoke:canvas-output`、`npm run smoke:runtime:strict`、`npm run smoke:anomaly`、最终 `npm run build` 全部通过。下一步继续围绕真实无限画布 RUN 后的产物回写和阶段推进做防错与联调。
- 2026-05-09 18:42 智能视界 canvas-output 回写防错增强：生产就绪总控的画布产物回写表单新增当前阶段输出目标预设选择、复制回写 JSON、写入结果打开产物入口，减少真实画布 RUN 后手填 artifactPath 错误并方便复查 Artifact Preview。最新验证：`npm run build`、`npm run smoke:canvas-output`、`npm run smoke:runtime:strict` 全部通过，Diagnostics ready / 0 issues。下一步用真实无限画布 RUN 的实际产物做一次人工回写验证。
- 2026-05-09 19:03 智能视界 Canvas RUN 会话状态源：新增 `outputs/.smart-vision/canvas-run-ledger.json` 记录真实无限画布 RUN 会话；Bridge 新增 RUN 会话 start/status API；`canvas-output` 回写支持 `runSessionId` 并自动把会话推进到 `output_registered`，对应 review done 后收敛为 `done`；生产就绪总控新增画布 RUN 会话统计、当前会话展示和创建 RUN 会话入口。最新验证：`node --check app/scripts/bridge-server.mjs`、`node --check app/scripts/canvas-output-smoke.mjs`、`npm run build`、`npm run smoke:canvas-output`、`npm run smoke:runtime:strict`、`npm run smoke:anomaly`、最终 `npm run build` 全部通过，Diagnostics ready / 0 issues。下一步用真实画布 RUN 产物做一次人工回写验证。
- 2026-05-09 19:15 智能视界 Canvas RUN Ledger Diagnostics：Runtime Diagnostics 已覆盖 `canvas-run-ledger.json` 的 sessions/events 结构、重复 session、非法状态、workflow 缺失、artifact 文件缺失、review 缺失和 canvasOutput 关联缺失；异常注入 smoke 新增 `canvas_run_ledger_corruption` 场景。最新验证：`node --check app/scripts/bridge-server.mjs`、`npm run build`、`npm run smoke:anomaly`、`npm run smoke:runtime:strict` 全部通过，Diagnostics ready / 0 issues。下一步用真实画布 RUN 产物做一次人工回写验证。

- 已建立创作源输入与影片风格画质基调前置体系：工作流开端不再默认写死为剧本输入，而是统一为“创作源输入 / 项目立项输入”，支持一句话创意、故事梗概、小说原文、已有剧本、角色设定、世界观、从0到1原创、已有分镜/资产等多入口；系统先判断创作成熟度 L0-L5，再决定故事开发线或制作执行线。开剧前必须做影片风格画质基调筛选，输出 Top3 推荐并最终锁定项目级 `visual-style-bible.md`；HZW大师风格被重定位为可作为主风格或质感控制方法，不再是唯一风格。核心文件为 `docs/creative-source-intake-workflow.md`、`docs/film-style-tone-system.md`、`docs/scene-type-style-library.md`、`docs/film-style-bible-standard.md`、`wordlists/style/film-style-tone-presets.json`、`wordlists/style/scene-type-presets.json`、`wordlists/style/style-recommendation-rules.json`、`templates/_shared/intake/`、`templates/_shared/style/`。
- 已建立当前中游新标准：中游主交付为“故事板生产包”，包括分镜表、D12-Sora2 / D15-Seedance2 专业导演故事板图、Panel 时长、连续性卡、资产引用矩阵、参考图职责矩阵和视频模型适配说明；不再把 Seedance/即梦/叙事版派生稿或视频提示词长版母稿定义为中游输出。提示词只作为围绕故事板、资产图、参考图和最终视频生成的配套生成说明，必须从故事板生产包派生。核心文件为 `docs/workflow.md`、`docs/phase-gate-checklist.md`、`docs/agent-protocol.md`、`docs/project-memory-system.md`、相关 `agents/` 与 `skills/`。
- 已建立场景设定稿交付体系：每个剧本核心大场景必须先从剧情中提取完整空间、设施、道具、建筑、群众/NPC/怪物/动物与主角站位，再使用 `gpt-image-2` 生成 `720度全景场景母图`，随后基于该母图生成 `12宫格固定视角图`。两张图共同作为最终场景交付物，并成为后续人物站位、分镜、导演故事板图、视频提示词和镜头调度的空间依据。核心文件为 `docs/scene-design-sheet-spec.md`、`wordlists/mj-image/scene-design-sheet.json`、`skills/art-design-skill.md`、`docs/workflow.md`、`docs/phase-gate-checklist.md`、`templates/_project-template/03-assets/scenes/scene-index.md`、`templates/_manga-template/03-assets/scenes/scene-index.md`。

## 《无限强化·灵纹觉醒》项目状态（2026-04-17 更新）
- 剧本正文：第 1-15 话全部完成
- 分镜表（storyboard）：ep001-ep010 全部完成
- Seedance 提示词（shots + fullref 15s）：ep001-ep010 全部完成
- Omni-paste 提示词（即梦/seedance/叙事）：ep001-ep010 全部完成
- 视觉资产：V7 全 15 项完成，V8 仅 1/15，全局 SREF 已锁定（林天觉醒后-v3-3）
- 漫画面板生成：ep001-ep004 部分镜头已生成（14张），ep005-010 = 0 张
- 分格脚本（漫剧 panel script）：第 1-5 话完成
- QA 关键问题：V8 资产缺 14/15、金纹战甲缺三态差分、练武场缺三态差分、面板生成滞后
- 下一优先：确认 V8 策略 → 三态差分 → ep005-010 面板生成 → 第 3 章创作
