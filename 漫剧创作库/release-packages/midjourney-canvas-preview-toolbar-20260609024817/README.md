# midjourney-canvas-preview-toolbar-20260609024817

Canvas-only package for `image-studio-canvas-next.html`.

- Adds Midjourney-specific toolbar presets and runtime params.
- Adds Midjourney four-grid preview and pager state.
- Preserves extension hooks for Describe / Blend / Action / Modal.
- Installs both public and mirror canvas HTML when those paths exist.
